# QA Agent Postman PoC (postPet)

Generates a Postman Collection v2.1 (`collection.json`) from `testcases.json` using `postman-collection`.

## Usage

```bash
npm install
npm run build
```

## Notes

- `testcases.json` must be in this folder.
- Output file: `collection.json`
